generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL") // Ye line missing thi
}

model Todo {
  id          Int      @id @default(autoincrement())
  title       String
  description String
}